{
  "name": "@umbra/backend",
  "version": "0.1.0",
  "private": true,
  "description": "Umbra fleet/admin backend — collector ingest API + server-side scoring",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/migrate.js",
    "seed": "node src/seed.js",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down"
  },
  "dependencies": {
    "@umbra/core": "*",
    "@workos-inc/node": "*",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
